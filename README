### README
# various setup dot-files

# to get this repository, go into $HOME:
#cd
# and clone it:
#git clone https://github.com/gitliver/.dotfiles.git
# it will create a directory: .dotfiles/

# To make it active, link the following dot files in $HOME:
#cd
#ln -s .dotfiles/.bash_profile 
#ln -s .dotfiles/.screenrc
#ln -s .dotfiles/.vimrc 
#ln -s .dotfiles/.gitconfig 

# also, to make sure screen works, make sure .bashrc sources .bash_profile:
#cat ~/.bashrc
# source ~/.bash_profile
