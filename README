### README
# various setup dot-files

# To make these active, link them in $HOME:
#cd
#ln -s .dotfiles/.bash_profile 
#ln -s .dotfiles/.screenrc
#ln -s .dotfiles/.vimrc 
#ln -s .dotfiles/.gitconfig 

# also, to make sure screen works, make sure .bashrc sources .bash_profile:
#cat ~/.bashrc
# source ~/.bash_profile
